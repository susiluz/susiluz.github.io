'use strict';

/* App Module */
var app = angular.module('emailGenerator', []);

/*var root = ''; */

/*
 *
 Define application routing
 *
 */
/*myApp.config(['$routeProvider', '$httpProvider',
    function ($routeProvider, $httpProvider) {
        var environment; 

        $httpProvider.defaults.cache = false;
        if (!$httpProvider.defaults.headers.get) {
            $httpProvider.defaults.headers.get = {};
        }
        $httpProvider.defaults.headers.get['If-Modified-Since'] = '0'; // disable IE ajax request caching

        $routeProvider.
        when('/template1', {
            templateUrl: root + 'generator/views/template1-generator.html',
            controller: 'PaperlessController'
        }).
        when('/template2', {
            templateUrl: root + 'generator/views/template2-generator.html',
            controller: 'PaperlessController'
        }).
        otherwise({
            redirectTo: '/template1'
        });


    }
]);

*/

